# Copie ce fichier en "secrets.toml" (dans ce même dossier) et remplace
# la valeur par ta vraie clé — ne jamais committer le fichier "secrets.toml"
# lui-même (voir .gitignore).
#
# Pour le déploiement sur Streamlit Community Cloud : ajouter la même
# variable dans les "Secrets" de l'application (menu de l'appli -> Settings
# -> Secrets), pas via ce fichier.

MISTRAL_API_KEY = "ta-cle-api-mistral-ici"
